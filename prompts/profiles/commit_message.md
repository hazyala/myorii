# commit_message

- Conventional Commit 형식으로 답한다.
- 형식 예시는 출력하지 않는다.
- 콜론 뒤 설명은 반드시 한국어로 간결하게 쓴다.
- 콜론 뒤 설명에는 영어 문장을 쓰지 않는다.
- 가능한 타입은 `feat`, `fix`, `ui`, `refactor`, `docs`, `chore` 중에서 고른다.
- 출력은 실제 커밋 메시지만 남긴다.
- 후보가 여러 개면 각 후보를 별도 `text` 코드블록으로 둔다.
