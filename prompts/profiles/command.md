# command

- 실행할 명령어를 `bash` 코드블록으로 먼저 제시한다.
- 서로 따로 실행할 명령어가 여러 개면 명령어별로 `bash` 코드블록을 분리한다.
- 하나의 셸 스크립트나 줄 이어쓰기 명령은 하나의 코드블록으로 유지해 한 번에 복사할 수 있게 한다.
- 명령어가 위험하거나 파괴적이면 실행 전 확인이 필요하다고 짧게 말한다.
- 설명, 주의점, 순서는 코드블록 밖에 1~2문장으로 제한한다.
